<h1><%= @feature.project.name %><h1>
<h2><%= @feature.name %></h2>
<h3>Description:<h3>
<p><%= @feature.description %></p>

<h3>Assigned Developer: <%= @dev %></h3>

<% if @dev == 'Not Assigned' %>

	<h3>Requests Received for Feature:</h3>

	<% if @feature.requests.any? %>
		<% @feature.requests.each do |request| %>
			<%= request.user.email %>
			<%= link_to 'Select', request_path(request), method: :patch %>
		<% end %>
	<% else %>
		No requests received yet
	<% end %>
<% end %>